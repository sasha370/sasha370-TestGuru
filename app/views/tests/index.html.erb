<h1 class="text-center"> <%= t('tests.index.header') %> </h1>

<table class="table">
  <thead class="bg-primary text-light">
  <tr>
    <th scope="col">#</th>
    <th scope="col"><%= t('activerecord.atributes.test.title') %></th>
    <th scope="col"><%= t('activerecord.atributes.test.level') %></th>
    <th scope="col"><%= t('activerecord.atributes.test.category_id') %></th>
    <th scope="col"><%= t('helpers.table_col.questions') %></th>
    <th scope="col"><%= t('helpers.buttons.start') %></th>
  </tr>
  </thead>
  <tbody>
  <%= render @tests %>
  </tbody>
</table>
