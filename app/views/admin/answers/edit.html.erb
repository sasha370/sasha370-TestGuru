<div class="card">
  <div class="card-header">
    <%= answer_header(@answer) %>
  </div>
  <div class="card-body">
    <%= render 'form', answer: @answer %>
  </div>
  <div class="card-footer">
    <%= link_to 'Назад к вопросу', admin_question_path(@answer.question), class: 'btn btn-sm btn-secondary' %>
  </div>
</div>
