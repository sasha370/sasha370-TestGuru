<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('helpers.links.show'), admin_question_path(question) %></td>
  <td><%= link_to t('helpers.links.edit'), edit_admin_question_path(question) %></td>
  <td><%= link_to t('helpers.links.delete'), admin_question_path(question), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
